version: 2.1

jobs:

  say-hello:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo "My first yaml"

  say-hi:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo "My second job"

  test-variable:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo $PRACTICE_VARIABLE
  
workflows:
  first-and-second:
    jobs:
        context:
          - practice_context
          - say-hello
          - say-hi